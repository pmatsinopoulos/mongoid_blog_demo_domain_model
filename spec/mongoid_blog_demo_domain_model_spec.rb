require 'spec_helper'

describe MongoidBlogDemoDomainModel do
  it 'has a version number' do
    expect(MongoidBlogDemoDomainModel::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
